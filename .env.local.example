# Root .env.local — used by the seed script only.
# Copy to .env.local and fill in your service account credentials.

# Option A: path to a downloaded service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Option B: individual fields from the service account JSON
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=
